/**
 * @type AuthStatus
 * @description Estados de autenticación de un usuario
 * @property {'checking' | 'authenticated' | 'not-authenticated'}
 */
export type AuthStatus = 'checking' | 'authenticated' | 'not-authenticated';

